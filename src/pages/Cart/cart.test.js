import { render, screen } from "__tests__/test-utils";
import Cart from "./Cart";

describe("Testing Cart component", () => {
  test("List correct number of elements on cart", () => {
    render(<Cart />);
    expect(false).toBe(true);
  });
});
